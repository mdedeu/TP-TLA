main {
    // Crear un árbol de N nodos utilizando un bucle o iterador.
    int nodeCount;
    tree int arbol;
    string names[6] = {"a","b","c","d","e","f"};
    
    for(nodeCount = 0;nodeCount < 6 ; nodeCount= nodeCount+1){
        arbol.createNode(names[abc], nodeCount);
    }
}