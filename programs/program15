main { 
    //  La creación de un vector con variables de diferente tipo.
    tree int arbol;
    node int nodito = arbol.createNode("root","rootId");

    int vector[5] = {"hola", 3, "messi", nodito};
}